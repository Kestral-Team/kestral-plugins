# Kestral MCP binary

This directory contains the pre-built `kestral-mcp` macOS universal binary. It is also available as a
[GitHub Release asset](https://github.com/Kestral-Team/kestral-plugins/releases).

The setup script's `--go-mcp` flag installs from this path (or downloads the matching release asset)
when Node.js is unavailable.
