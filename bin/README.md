# Kestral helper

This directory contains the pre-built `kestral-mcp` macOS universal executable. It is also available as a
[GitHub Release asset](https://github.com/Kestral-Team/kestral-plugins/releases).

The setup script installs from this path when present, or downloads the latest release asset if missing.
